// 修改成你期望的宽度
// 当屏幕宽度 < 1600px
$content-desktop = 1000px

// 当视窗超过 1600px 后的宽度
$content-desktop-large = 1500px

// 标题，修改成你期望的字体族
$font-family-headings = Georgia, sans

// 修改成你期望的字体族
$font-family-base = "Microsoft YaHei", Verdana, sans-serif

// 代码字体
$code-font-family = "Input Mono", "PT Mono", Consolas, Monaco, Menlo, monospace

// 正文字体的大小
$font-size-base = 20px

// 代码字体的大小
$code-font-size = 18px

// Headings font size
$font-size-headings-step    = 2px
$font-size-headings-base    = 28px  //这个是标题大小，如果你觉得不满意，可以改的更大一点
